```bash
echo "# html_refresher" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/SharadwiMesa/html_refresher.git
git push -u origin master
git remote add origin https://github.com/SharadwiMesa/html_refresher.git
git push -u origin master
```